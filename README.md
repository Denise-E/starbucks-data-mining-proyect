# Starbucks Data Mining Project

Data Mining analysis of Starbucks customer ordering patterns using Decision Trees and Clustering (WEKA + Python).

**Universidad de Palermo** — Análisis de la Información y la Decisión

---

## Business Problem

Building on our previous Data Warehouse project, we identified that **Drive-Thru is the main operational bottleneck** (5.79 min avg vs. 3.22 min baseline). This project goes one step further: instead of describing what happened, we use Data Mining to **predict and segment**:

- **Decision Tree:** What factors determine whether a customer ends up satisfied or dissatisfied?
- **Clustering:** What behavioral customer profiles exist, and how do they differ?

---

## Project Architecture

```
starbucks-data-mining-proyect/
│
├── data/
│   └── starbucks_customer_ordering_patterns.csv   ← Raw dataset from Kaggle (source of truth)
│
├── notebooks/
│   ├── 01_eda.ipynb                               ← Exploratory Data Analysis: distributions, nulls, outliers
│   ├── 02_preprocessing.ipynb                     ← Cleaning, discretization, normalization, ARFF export
│   └── 03_results_analysis.ipynb                  ← Visualize and interpret model outputs
│
├── weka/
│   ├── starbucks_dm.arff                          ← Input file generated by notebook 02, consumed by WEKA
│   └── results/                                   ← WEKA exported outputs (tree rules, cluster assignments)
│
└── presentation/
    └── ...                                        ← Final PPT and supporting assets for the demo
```

### Data flow

```
data/CSV  →  notebooks/02 (preprocessing)  →  weka/ARFF  →  WEKA models
                                                                    ↓
                                            notebooks/03 (results analysis + charts)
                                                                    ↓
                                                         presentation/PPT
```

---

## Methods

| Method | Algorithm | Tool | Target |
|---|---|---|---|
| Decision Tree | J48 (C4.5) | WEKA | `customer_satisfaction` → High (≥4) / Low (≤3) |
| Clustering | SimpleKMeans | WEKA | Customer behavioral segments |

**Key features used:** `order_channel`, `fulfillment_time_min`, `cart_size`, `num_customizations`, `has_food_item`, `is_rewards_member`, `store_location_type`, `time_period`

---

## How to Run

1. Open notebooks in order: `01_eda` → `02_preprocessing` → `03_results_analysis`
2. `02_preprocessing` will generate `weka/starbucks_dm.arff`
3. Load the ARFF in WEKA Explorer → run J48 and SimpleKMeans
4. Export WEKA results to `weka/results/`
5. Run `03_results_analysis` to generate final charts

---

## Context

This project is a continuation of the **Data Warehouse TP** built on the same dataset.  
The DW established the Star Schema (PostgreSQL) and answered descriptive business questions via Power BI.  
The DM layer adds predictive and segmentation capabilities on top of those findings.
